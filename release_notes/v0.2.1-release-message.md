# v0.2.1 -- `prob_floor` on the continuous engine (delta_p_min knob)

`dcl_core v0.2.1` is a single-feature patch: the **`prob_floor`**
parameter on `dcl_core.core.CausalSession`.  It exists to unblock
`dcl-delta-p-min` -- the `core` column of its 4-cell grid
(`exp_09_dp_min_floor`) cannot run without a continuous-engine
`delta_p_min` knob -- by giving that investigation a pinnable artifact
(`dcl_core @ git+...@v0.2.1`).

**This is not v1.0.0.**  The formal API freeze remains gated on
`dcl-delta-p-min` finishing and co-releases with Paper III.

**Semver:** PATCH.  `prob_floor` is purely opt-in and
backward-compatible -- `prob_floor=None` (default) is bit-for-bit the
v0.2.0 engine; no re-exported symbol is added or removed.

## What `prob_floor` does

```python
from dcl_core.core import CausalSession
s = CausalSession(lattice, node, omega, prob_floor=1e-6)
```

Per-site probability is `p(x) = |psi_R|^2 + |psi_L|^2`.  At
end-of-tick, any node with `0 < p(x) < prob_floor` is scaled up so
`p(x) == prob_floor` exactly (phase and the `psi_R/psi_L` ratio
preserved); the joint state is then renormalised to `sum(p) == 1`, so
A=1 is never broken.  Empty nodes (`p == 0`) stay empty -- the floor
is the minimum *non-zero* probability quantum, the continuous analogue
of `core3d`'s `1/N`.  `None` (default) clamps nothing.

## Tests

`pytest tests/` -> **252 passed, 26 xfailed, 0 skipped** (v0.2.0: 232
passed).  The +20 are `tests/test_prob_floor_a1_consistency.py`:
validation, backward-compat (bitwise), exact clamp, phase/ratio
preservation, zero-site-stays-zero, and A=1 after every tick across a
decade sweep of `prob_floor`.

A diagnostic, `experiments/exp_03_prob_floor_diag.py`, sweeps the floor
and confirms `max|A-1| ~ 1e-16` at every value.

## Installing

```sh
pip install "git+https://github.com/JackDMenendez/dcl-core@v0.2.1"
```

## What is NOT in v0.2.1

Unchanged from v0.2.0's deferral list: GPU kernels, inter-session
coupling, complex `TokenResidual` carry.  Also out of scope for
`prob_floor` itself: per-edge/per-plaquette floors, a complementary
`prob_ceiling` (the unity renorm restores the high side), and a
`prob_floor` on `core3d` (its quantum is `N` alone).

---

**Citation.** See `CITATION.cff`.  DOI:
[10.5281/zenodo.20615410](https://doi.org/10.5281/zenodo.20615410).
